import { PhasePlaceholder } from "@/components/ui/PhasePlaceholder";

export default function WorkflowDesignerPage() {
  return (
    <PhasePlaceholder
      phase="Phase 3–4"
      title="Workflow Designer"
      description="Design future-state workflows with AI agents, decision branches, and human approval gates using React Flow."
    />
  );
}
