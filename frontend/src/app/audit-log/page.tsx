import { PhasePlaceholder } from "@/components/ui/PhasePlaceholder";

export default function AuditLogPage() {
  return (
    <PhasePlaceholder
      phase="Phase 5"
      title="Audit Log"
      description="Chronological, explainable audit trail for every AI action, human decision, and workflow transition."
    />
  );
}
