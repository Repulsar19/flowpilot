import { PhasePlaceholder } from "@/components/ui/PhasePlaceholder";

export default function ApprovalsPage() {
  return (
    <PhasePlaceholder
      phase="Phase 4"
      title="Approvals"
      description="Review AI recommendations with confidence, reasoning, and SOP evidence before approving, rejecting, or requesting review."
    />
  );
}
