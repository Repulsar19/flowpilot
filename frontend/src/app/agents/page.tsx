import { PhasePlaceholder } from "@/components/ui/PhasePlaceholder";

export default function AgentsPage() {
  return (
    <PhasePlaceholder
      phase="Phase 4"
      title="AI Agents"
      description="Manage agent definitions including tools, permissions, confidence thresholds, escalation rules, and approval requirements."
    />
  );
}
