import { PhasePlaceholder } from "@/components/ui/PhasePlaceholder";

export default function SettingsPage() {
  return (
    <PhasePlaceholder
      phase="Phase 6"
      title="Settings"
      description="Configure API keys, model selection, governance defaults, and deployment environment variables."
    />
  );
}
